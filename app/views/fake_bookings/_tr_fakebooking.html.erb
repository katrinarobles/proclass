  <td id="fakes-<%= fake_booking.id %>"> <%= fake_booking.fake_first_name.capitalize %></td>
  <td> <%= fake_booking.fake_last_name.capitalize %> </td>
  <td style="display: flex; justify-content: space-between; padding-right: 5%;"> <%= fake_booking.fake_email if fake_booking.fake_email %>
       <span> <%= link_to course_occurence_fake_booking_path(@occurence.course, @occurence, fake_booking),
            page: 'occurences/show',
            method: :delete,
            remote: true,
            data: { confirm: "Are you sure you'd like to cancel?" } do %> <i class="far fa-trash-alt"></i> <% end %>
       </span>
  </td>
