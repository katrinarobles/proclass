document.querySelector(`.occurence-home-cards[data-occurid='<%= @occurence.id %>']`).innerHTML = `<%= render partial: 'occurences/occurence_card', locals: {occurence: @occurence, booking: @booking, page: 'home'} %>`
document.querySelector(`.info-wdw`).innerHTML = `<%= render partial: 'pages/info_window', locals: {occurence: @occurence, booking: @booking, page: 'home'} %>`
